# evaluate.py — evaluation harness (chunks 10 & 11)
# runs all components on the golden set and computes metrics

def run_evaluation(golden_path, output_path="outputs/metrics.json"):
    """run full evaluation pipeline on the golden set. placeholder."""
    raise NotImplementedError("implement in chunks 10/11")
